# ── 파이썬 ──────────────────────────────────────────
__pycache__/
*.py[cod]
.venv/
venv/

# ── macOS ──────────────────────────────────────────
.DS_Store
._*
.Spotlight-V100
.Trashes

# ── 편집기 ──────────────────────────────────────────
.vscode/
.idea/
*.swp

# ── 촬영 원본 ───────────────────────────────────────
# 원본 199장(약 1.5GB)은 Desktop/king-원본사진/ 에 따로 있습니다.
# 웹사이트 구동에 필요 없고 저장소만 무거워지므로 올리지 않습니다.
# 사진을 다시 뽑을 때만 tools/rebuild_photos.py 가 그 폴더를 씁니다.
king-원본사진/

# 작업용 원본(캐릭터 PNG · 음식 스튜디오컷 55MB) — 사이트 구동에 쓰이지 않습니다
_original-photos/

# npm 패키지 (npm install 로 다시 받을 수 있습니다)
node_modules/
